import { ImageList } from "@/features/ImageList/ImageList";

export const HomePage = () => {
  return (
    <div className="w-full min-h-screen flex justify-center items-center bg-neutral-200">
      <ImageList />
    </div>
  );
};
